# Project 1
This was my first project for my COMP 484 course about HTML, CSS, and JavaScript.

The project was to create a website with HTML and CSS using three unique HTML elements and three unique HTML attributes.

## Elements
For this project, I used <template>, <datalist>, and <output> as my unique elements.

* The <template> element was used to hide content until a button was clicked, with JavaScript used to reveal the content.
* The <datalist> element was used to provide a list of boss names as suggestions for the search input.
* The <output> element was used to display the result of the short quiz.

## Attributes
For the attributes, I used <autocomplete>, <enterkeyhint>, and <inputmode>.

* autocomplete:
  * Was used to allow the browser to suggest previously entered values in the search input.
* enterkeyhint:
  * Was used to indicate that the Enter key should display a ‘Go’ action on supported keyboards.
* inputmode:
  * Was used to tell mobile devices to display a search-optimized keyboard when the input is selected.

**GitHub Pages Link:** 
[https://m1chaelguerrer0.github.io/MGS-3-Website/](https://m1chaelguerrer0.github.io/MGS-3-Website/)